/**
 * Legacy full-screen loader — disabled so it cannot cover the viewport (blank screen).
 * Re-enable only if you restore a timed fade-out and z-index that always clears.
 */
const PreloaderPrimary = () => {
  return null;
};

export default PreloaderPrimary;
