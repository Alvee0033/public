export const validateStudentInfo = (studentInfo) => {
  const errors = {};
  
  // All fields are now optional - no validation required
  
  return {
    isValid: true, // Always valid since everything is optional
    errors
  };
};

export const validateGuardianInfo = (guardianInfo) => {
  const errors = {};
  
  // All fields are now optional - no validation required
  
  return {
    isValid: true, // Always valid since everything is optional
    errors
  };
};

export const validateLearningGoals = (learningGoals) => {
  const errors = {};
  
  // All fields are now optional - no validation required
  
  return {
    isValid: true, // Always valid since everything is optional
    errors
  };
};

export const validateCourseSelection = (courseSelection) => {
  const errors = {};
  
  // All fields are now optional - no validation required
  
  return {
    isValid: true, // Always valid since everything is optional
    errors
  };
};

export const validateFinancialInfo = (financialInfo) => {
  const errors = {};
  
  // All fields are now optional - no validation required
  
  return {
    isValid: true, // Always valid since everything is optional
    errors
  };
};

export const validateApplicationEssay = (applicationEssay) => {
  const errors = {};
  
  // All fields are now optional - no validation required
  
  return {
    isValid: true, // Always valid since everything is optional
    errors
  };
};

export const validateAcademicSupport = (academicSupport) => {
  const errors = {};
  
  // All fields are now optional - no validation required
  
  return {
    isValid: true, // Always valid since everything is optional
    errors
  };
};